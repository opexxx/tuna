<% @mail_object = Mail.new(@email.fulltext); nil %>

<p>
  <b>Domain:</b>
  <%=h 	@email.domain %>
</p>

<p>
  <b>Fingerprints:</b><br/>
  <ul>
  <% @email.fingerprint_matches.each do |match| %>
  <li><a href=<%=h fingerprint_path(match.fingerprint)%>><%=h match.fingerprint.name + " (/" + match.fingerprint.regex + "/)"%></a></li>
  <% end %>
  </ul>
</p>

<p>
  <b>Path:</b>
  <%=h 	@email.path.split(" ").reverse.join (" -> ") %>
</p>

<p>
  <b>MX Records (Forward DNS):</b>
  <%=h @email.grab_mx  %>
</p>

<p>
  <b>From:</b>
  <%=h @mail_object.from %>
</p>

<p>
  <b>Text:</b>
  <pre>
  <%=h @email.fulltext %>
  </pre>
</p>


<%= link_to 'Edit', edit_email_path(@email) %> |
<%= link_to 'Back', emails_path %>
